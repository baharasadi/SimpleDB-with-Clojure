(ns simpledb-with-clojure.pipeline)

(defn map-transducer [f]
  (fn [rf]
    ;; TODO
    ))
(defn group-by-transducer [keys]
  (fn [rf]
    ;; TODO
    ))
(defn filter-transducer [pred]
  (fn [rf]
    ;; TODO
    ))
(defn sum-transducer []
  (fn [rf]
    ;; TODO
    ))
(defn count-transducer []
  (fn [rf]
    ;; TODO
    ))
